apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-first-chart.fullname" . }}
  labels:
    {{- include "test-first-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-first-chart.selectorLabels" . | nindent 4 }}
